# PSX Investment Engine — deployment bundle

Deployment-only static frontend for the PSX Investment Engine.

This repository contains browser-safe assets only. It contains no private research data, database migrations, or server-side credentials.

The Supabase publishable key in `config.js` is intended for browser use and access is governed by Row Level Security.
